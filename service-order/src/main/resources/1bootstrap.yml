spring:
  cloud:
    config:
      uri: http://localhost:9010

  application:
    name: orderService
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: *

